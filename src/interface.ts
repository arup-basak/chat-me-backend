export interface Message {
    message: string
    time: string
    user: string
}

export interface ActiveUser {
    name: string,
    id: string
}

export interface User {
    name: string
}

export interface Group {
    name: string,
}